@model IEnumerable<Asp.net_Core_MVC_GestionMag.Models.Article>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomArticle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionArticle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QtiteArticle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomArticle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionArticle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QtiteArticle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
